#include <stdio.h>

/**
 * print _putchar
 * followed by a new line
 * Return: always 0 if successful
 */

int main(void)
{
	char text[9] = "_putchar";
	int i = 0;

	for (i = 0; i < 8; i++)
	{
		putchar(text[i]);
	}
	_putchar('\n');

	return (0);
}
